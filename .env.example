# Environment Configuration
# Set to 'staging' to show API base URL configuration popup
# Set to 'production' to use VITE_API_BASE_URL directly
VITE_ENVIRONMENT=staging

# API Base URL (used when VITE_ENVIRONMENT=production)
# Example: https://yourdomain.com/podcore
VITE_API_BASE_URL=https://testhostharan.leapmile.com/podcore
