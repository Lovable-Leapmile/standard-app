# Environment Configuration
# Note: API Base URL configuration is now handled through the UI popup
# These variables are kept for reference only

# Environment type (staging or production)
VITE_ENVIRONMENT=production

# API Base URL (for reference - actual URL is configured via UI popup on first launch)
# Example: https://yourdomain.com/podcore
VITE_API_BASE_URL=https://testhostharan.leapmile.com/podcore
