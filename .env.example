# Environment Configuration

# Environment type (staging or production)
# - production: Uses VITE_API_BASE_URL automatically, no popup shown
# - staging: Shows popup to configure API base URL dynamically
VITE_ENVIRONMENT=production

# API Base URL (used automatically when VITE_ENVIRONMENT=production)
# Example: https://yourdomain.com/podcore
VITE_API_BASE_URL=https://testhostharan.leapmile.com/podcore
