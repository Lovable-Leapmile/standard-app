# =================================================================
# IMPORTANT: This is just a TEMPLATE file
# To actually configure the app, copy this to .env (for dev) or
# use .env.production (for production builds)
# =================================================================

# Environment type (staging or production)
VITE_ENVIRONMENT=production

# API Base URL (used only in production)
VITE_API_BASE_URL=https://productionv36.qikpod.com/podcore

# =================================================================
# ENVIRONMENT FILES EXPLAINED:
# - .env → Used during "npm run dev" (local development)
# - .env.production → Used during "npm run build" (production build)
# - .env.staging → Used during "npm run build -- --mode staging"
# =================================================================
